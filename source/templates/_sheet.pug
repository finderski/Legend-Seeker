// Necessary Mixins
include ./mixins/_mixin_attributes.pug 

// Hidden inputs for character sheet functionality

.maindiv
    .mancer 
        button(type='action', class='mancerButton', name='act_characterMancer' data-i18n-title='character-mancer')
    h1(class='sheetTitle') Gamma World 2e
    input(type='hidden', class='selectedTab', name='attr_sheetTab', value="1")
    include _demographics.pug
    .buttonbar 
        // Buttons to control the sheet display
        include _buttonbar.pug
    // input(type='hidden', class='selectedTab', name='attr_sheetTab', value="1")
    .character 
        // Character Sheet (including NPC sheet)
        include _character.pug
    .characternotes 
        // Character Notes Section
        include _characternotes.pug
    .gm-screen 
        // GM Screen Section
        include _gm_screen.pug
    // .releaseNotes 
        Release Notes Section
        include _release_notes.pug
    // .sheetDocumentation 
        Sheet Documentation Section
        include _sheet_documentation.pug

//Rolltemplates
include ../html/rolltemplate.html